apiVersion: v1
kind: Secret
metadata:
  name: {{ include "k8s.fullname" . }}-rsa
  labels:
    {{- include "k8s.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.rsaKeys }}
  {{ $key }}: {{ $val | quote }}
  {{- end}}
