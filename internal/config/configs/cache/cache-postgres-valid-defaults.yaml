server:
  port: 9095

app-storages:
  - name: postgres-storage-defaults
    postgres:
      url: postgres://user:pass@localhost:5432/db

cache:
  connectors:
    - driver: postgres
      id: pg1
      postgres:
        storage-name: postgres-storage-defaults

upstream-config:
  upstreams:
    - id: eth-upstream
      chain: ethereum
      connectors:
        - type: json-rpc
          url: https://test.com
