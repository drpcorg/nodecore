server:
  port: 9095

app-storages:
  - name: redis-storage-no-address
    redis: {}

cache:
  connectors:
    - driver: redis
      id: redis3
      redis:
        storage-name: redis-storage-no-address

upstream-config:
  upstreams:
    - id: eth-upstream
      chain: ethereum
      connectors:
        - type: json-rpc
          url: https://test.com
