server:
  port: 9095

cache:
  connectors:
    - driver: redis
      id: redis3
      redis: {}

upstream-config:
  upstreams:
    - id: eth-upstream
      chain: ethereum
      connectors:
        - type: json-rpc
          url: https://test.com
