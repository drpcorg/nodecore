server:
  port: 9095

app-storages:
  - name: redis-storage-negative-connect
    redis:
      address: localhost:6379
      timeouts:
        connect-timeout: -1s

cache:
  connectors:
    - driver: redis
      id: redis6
      redis:
        storage-name: redis-storage-negative-connect

upstream-config:
  upstreams:
    - id: eth-upstream
      chain: ethereum
      connectors:
        - type: json-rpc
          url: https://test.com
