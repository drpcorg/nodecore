server:
  port: 9095

cache:
  connectors:
    - driver: redis
      id: redis4
      redis:
        address: localhost:6379
        timeouts:
          read-timeout: -1s

upstream-config:
  upstreams:
    - id: eth-upstream
      chain: ethereum
      connectors:
        - type: json-rpc
          url: https://test.com
