server:
  port: 9095

cache:
  connectors:
    - driver: redis
      id: redis5
      redis:
        address: localhost:6379
        timeouts:
          write-timeout: -1s

upstream-config:
  upstreams:
    - id: eth-upstream
      chain: ethereum
      connectors:
        - type: json-rpc
          url: https://test.com
