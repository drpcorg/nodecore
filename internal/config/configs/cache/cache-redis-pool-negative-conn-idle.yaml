server:
  port: 9095

cache:
  connectors:
    - driver: redis
      id: redis12
      redis:
        address: localhost:6379
        pool:
          conn-max-idle-time: -1s

upstream-config:
  upstreams:
    - id: eth-upstream
      chain: ethereum
      connectors:
        - type: json-rpc
          url: https://test.com
