server:
  port: 9095

app-storages:
  - name: redis-storage-negative-pool-timeout
    redis:
      address: localhost:6379
      pool:
        pool-timeout: -1s

cache:
  connectors:
    - driver: redis
      id: redis8
      redis:
        storage-name: redis-storage-negative-pool-timeout

upstream-config:
  upstreams:
    - id: eth-upstream
      chain: ethereum
      connectors:
        - type: json-rpc
          url: https://test.com
