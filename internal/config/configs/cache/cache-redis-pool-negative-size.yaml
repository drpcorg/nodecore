server:
  port: 9095

app-storages:
  - name: redis-storage-negative-size
    redis:
      address: localhost:6379
      pool:
        size: -1

cache:
  connectors:
    - driver: redis
      id: redis7
      redis:
        storage-name: redis-storage-negative-size

upstream-config:
  upstreams:
    - id: eth-upstream
      chain: ethereum
      connectors:
        - type: json-rpc
          url: https://test.com
