server:
  port: 9095

cache:
  connectors:
    - driver: redis
      id: redis1
      redis:
        address: localhost:6379

upstream-config:
  upstreams:
    - id: eth-upstream
      chain: ethereum
      connectors:
        - type: json-rpc
          url: https://test.com
