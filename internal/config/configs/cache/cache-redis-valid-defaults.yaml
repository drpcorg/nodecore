server:
  port: 9095

app-storages:
  - name: redis-storage-defaults
    redis:
      address: localhost:6379

cache:
  connectors:
    - driver: redis
      id: redis1
      redis:
        storage-name: redis-storage-defaults

upstream-config:
  upstreams:
    - id: eth-upstream
      chain: ethereum
      connectors:
        - type: json-rpc
          url: https://test.com
