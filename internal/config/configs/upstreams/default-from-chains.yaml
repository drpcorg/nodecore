upstream-config:
  chain-defaults:
    ethereum:
      poll-interval: 10m
  upstreams:
    - id: eth-upstream
      chain: ethereum
      connectors:
        - type: json-rpc
          url: https://test.com