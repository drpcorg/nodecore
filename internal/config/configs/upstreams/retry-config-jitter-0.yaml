upstream-config:
  upstreams:
    - id: eth-upstream
      chain: polygon
      failsafe-config:
        retry:
          attempts: 3
          jitter: 0s
      connectors:
        - type: json-rpc
          url: https://test.com